{
  "name": "phimhay",
  "private": true,
  "scripts": {
    "dev:api": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client"
  },
  "dependencies": {
    "vite": "^8.0.3"
  }
}
